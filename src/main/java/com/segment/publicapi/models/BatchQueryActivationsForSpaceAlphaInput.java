/*
 * Segment Public API
 * The Segment Public API helps you manage your Segment Workspaces and its resources. You can use the API to perform CRUD (create, read, update, delete) operations at no extra charge. This includes working with resources such as Sources, Destinations, Warehouses, Tracking Plans, and the Segment Destinations and Sources Catalogs.  All CRUD endpoints in the API follow REST conventions and use standard HTTP methods. Different URL endpoints represent different resources in a Workspace.  See the next sections for more information on how to use the Segment Public API.
 *
 * Contact: friends@segment.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.segment.publicapi.models;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.segment.publicapi.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;

/**
 * Input to list activations in bulk across many audiences in one call (space-level read). Intended
 * for callers pulling activation metadata for many audiences on a recurring schedule, to avoid one
 * request per audience against the per-space rate limit.
 */
public class BatchQueryActivationsForSpaceAlphaInput {
    public static final String SERIALIZED_NAME_AUDIENCE_IDS = "audienceIds";

    @SerializedName(SERIALIZED_NAME_AUDIENCE_IDS)
    private List<String> audienceIds;

    public static final String SERIALIZED_NAME_PAGINATION = "pagination";

    @SerializedName(SERIALIZED_NAME_PAGINATION)
    private PaginationInput pagination;

    public BatchQueryActivationsForSpaceAlphaInput() {}

    public BatchQueryActivationsForSpaceAlphaInput audienceIds(List<String> audienceIds) {

        this.audienceIds = audienceIds;
        return this;
    }

    public BatchQueryActivationsForSpaceAlphaInput addAudienceIdsItem(String audienceIdsItem) {
        if (this.audienceIds == null) {
            this.audienceIds = new ArrayList<>();
        }
        this.audienceIds.add(audienceIdsItem);
        return this;
    }

    /**
     * Audience IDs to list activations for. Max 100 IDs per call. Duplicates are de-duplicated
     * server-side but still count toward the cap. Omit (or pass an empty array) to scan every
     * audience in the space instead - see the &#x60;pagination&#x60; doc below for how that changes
     * what a \&quot;page\&quot; means. Omitting this list is materially higher latency per call
     * than a scoped batch, since each page has to enumerate audiences before it can look up their
     * activations.
     *
     * @return audienceIds
     */
    @javax.annotation.Nullable
    public List<String> getAudienceIds() {
        return audienceIds;
    }

    public void setAudienceIds(List<String> audienceIds) {
        this.audienceIds = audienceIds;
    }

    public BatchQueryActivationsForSpaceAlphaInput pagination(PaginationInput pagination) {

        this.pagination = pagination;
        return this;
    }

    /**
     * Get pagination
     *
     * @return pagination
     */
    @javax.annotation.Nullable
    public PaginationInput getPagination() {
        return pagination;
    }

    public void setPagination(PaginationInput pagination) {
        this.pagination = pagination;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        BatchQueryActivationsForSpaceAlphaInput batchQueryActivationsForSpaceAlphaInput =
                (BatchQueryActivationsForSpaceAlphaInput) o;
        return Objects.equals(this.audienceIds, batchQueryActivationsForSpaceAlphaInput.audienceIds)
                && Objects.equals(
                        this.pagination, batchQueryActivationsForSpaceAlphaInput.pagination);
    }

    @Override
    public int hashCode() {
        return Objects.hash(audienceIds, pagination);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class BatchQueryActivationsForSpaceAlphaInput {\n");
        sb.append("    audienceIds: ").append(toIndentedString(audienceIds)).append("\n");
        sb.append("    pagination: ").append(toIndentedString(pagination)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("audienceIds");
        openapiFields.add("pagination");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to
     *     BatchQueryActivationsForSpaceAlphaInput
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!BatchQueryActivationsForSpaceAlphaInput.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in"
                                    + " BatchQueryActivationsForSpaceAlphaInput is not found in the"
                                    + " empty JSON string",
                                BatchQueryActivationsForSpaceAlphaInput.openapiRequiredFields
                                        .toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!BatchQueryActivationsForSpaceAlphaInput.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the"
                                    + " `BatchQueryActivationsForSpaceAlphaInput` properties. JSON:"
                                    + " %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the optional json data is an array if present
        if (jsonObj.get("audienceIds") != null
                && !jsonObj.get("audienceIds").isJsonNull()
                && !jsonObj.get("audienceIds").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            "Expected the field `audienceIds` to be an array in the JSON string but"
                                    + " got `%s`",
                            jsonObj.get("audienceIds").toString()));
        }
        // validate the optional field `pagination`
        if (jsonObj.get("pagination") != null && !jsonObj.get("pagination").isJsonNull()) {
            PaginationInput.validateJsonElement(jsonObj.get("pagination"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!BatchQueryActivationsForSpaceAlphaInput.class.isAssignableFrom(
                    type.getRawType())) {
                return null; // this class only serializes 'BatchQueryActivationsForSpaceAlphaInput'
                // and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<BatchQueryActivationsForSpaceAlphaInput> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(BatchQueryActivationsForSpaceAlphaInput.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<BatchQueryActivationsForSpaceAlphaInput>() {
                        @Override
                        public void write(
                                JsonWriter out, BatchQueryActivationsForSpaceAlphaInput value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public BatchQueryActivationsForSpaceAlphaInput read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of BatchQueryActivationsForSpaceAlphaInput given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of BatchQueryActivationsForSpaceAlphaInput
     * @throws IOException if the JSON string is invalid with respect to
     *     BatchQueryActivationsForSpaceAlphaInput
     */
    public static BatchQueryActivationsForSpaceAlphaInput fromJson(String jsonString)
            throws IOException {
        return JSON.getGson().fromJson(jsonString, BatchQueryActivationsForSpaceAlphaInput.class);
    }

    /**
     * Convert an instance of BatchQueryActivationsForSpaceAlphaInput to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
